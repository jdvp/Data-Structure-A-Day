/**
 * Created by JD on 8/20/2015.
 */
public interface IArrayOperation {
    /**
     * Computes a result for the desired operation (which will inherit this interface) and prints it.
     * A result is also returned in case it is needed for a further calculation.
     * @return
     */
    int[][] compute();
}
