/**
 * Created by JD on 9/28/2015.
 */
public interface Sorter {

    /**
     * Sorts the input array using the specific Sorting Algorithm
     * Note however, that we do not need to actually return the resultant array since the
     * array is sorted in place and is passed-by-reference.
     * @param input The input array to sort
     */
    void sort(int[] input);
}
